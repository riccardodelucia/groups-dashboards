version: "3.8"

services:
  groups-dashboards:
    image: "$CI_REGISTRY_IMAGE:$TAG"
    ports:
      - $PORT:80
    command: nginx
    env_file:
      - .env
    networks:
      app_net:
        ipv4_address: $IPV4_GROUPS_DASHBOARDS

networks:
  app_net:
    ipam:
      driver: default
      config:
        - subnet: $IPV4_SUBNET
          gateway: $IPV4_GATEWAY
